<%= form_for([@post, @post.comments.build]) do |f| %>

  <div class="field">
    <%= f.label :content, label "Reply to thread" %><br>
    <%= f.text_area :content %>
  </div>

  <div class="field">
    <%= f.label :content, label "Reply to thread" %><br>
    <%= f.text_area :content %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>